# Pattern Storage Directory

This directory stores saved Strudel patterns.

Patterns are stored as JSON files with metadata:
- Pattern code
- Tags for organization  
- Timestamp
- Audio analysis data (when available)

Created automatically if it does not exist.
